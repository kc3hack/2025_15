using UnityEngine;

public class TimeSave : MonoBehaviour
{
    private static TimeSave instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}